@model Drogueria1._1_.Models.Laboratorio

@{
    ViewData["Title"] = "Editar";
}

<div class="container mt-4">
    <h1>@ViewData["Title"]</h1>
    <hr />
    <div class="row">
        <div class="col-md-4">
            <form asp-action="Edit">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="mb-3">
                    <label asp-for="IdLaboratorio" class="form-label">ID Laboratorio</label>
                    <input asp-for="IdLaboratorio" class="form-control" readonly />
                </div>
                <div class="form-group">
                    <label asp-for="NombreLab" class="form-label">Nombre</label>
                    <input asp-for="NombreLab" class="form-control" />
                    <span asp-validation-for="NombreLab" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <button type="submit" class="btn btn-primary">Guardar</button>
                </div>
            </form>
        </div>
    </div>
    <div class="mt-3">
        <a asp-action="Index" class="btn btn-secondary">Regresar</a>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}